//! ahvm-forge: guest agent (PID 1 inside each microVM).
//!
//! Phase 2 target: static musl binary with a CI-enforced size budget,
//! speaking the v2 protocol ([`ahvm_proto`]). Replaces `cmd/forge`.

fn main() {
    eprintln!("ahvm-forge: not yet implemented (see docs/PLAN-rust-rewrite.md)");
}
