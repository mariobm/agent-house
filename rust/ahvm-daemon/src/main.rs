//! ahvm-daemon: control plane (REST/WS API, thermal manager, scheduler).
//!
//! Phase 4 target: axum-based, dashboard-first API (cursor pagination,
//! SSE event bus, OpenAPI from code). Replaces `cmd/ahvm serve` +
//! `pkg/server`.

fn main() {
    eprintln!("ahvm-daemon: not yet implemented (see docs/PLAN-rust-rewrite.md)");
}
